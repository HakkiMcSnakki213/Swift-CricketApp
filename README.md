# Swift-CricketApp
A Mobile application that manages cricket matches and player data with persistance using firebase. This is 1 of 3 different versions of the application with this being the Swift (iOS) version.
